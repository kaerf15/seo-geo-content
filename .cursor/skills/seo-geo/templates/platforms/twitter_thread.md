<!--
叙事层（Agent 必读）
原型：#8 Crisp Thread（锋利线程）
手册：`_narrative_playbook.md`
差异化检验：一条一句话 / 一条一个锋利观点；禁止连续两条讲同一件事；数字/对比/冲突穿插。
-->

# Twitter / X 线程

> 条数区间：`platform_rules.yaml#twitter_thread.items`（硬范围 `min`/`max`；推荐落在 `recommended_min`–`recommended_max`）。
> 每条字符上限：`platform_rules.yaml#twitter_thread.per_item_chars.max`。
>
> **占位规则**：下方只给首/尾两条锚点 + 一条中间样板，**具体条数以 rules 推荐区间为准**——不要看到骨架写了几条就定稿几条。Agent 动笔前先读 rules 的 `items` 段，按推荐区间补齐中间要点。

1/ （首条钩子：一句话结论，制造打开欲）

2/ （要点 + 数字；每条一句话，禁止连续两条讲同一件事）

… （按 `items.recommended_min`–`items.recommended_max` 补齐中间要点，每条 ≤ `per_item_chars.max` 字符）

N/ （结尾 CTA：关注 / 收藏 / 链接）
